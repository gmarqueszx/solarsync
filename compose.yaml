services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=solarsync'
      - 'POSTGRES_USER=solarsync'
      - 'POSTGRES_PASSWORD=solarsync_dev'
    ports:
      - '5432'
